# DOX — hermes-plugin/tests

| File | Purpose |
| ------ | --------- |
| `test_choice_parser.py` | Explicit, ambiguous, negated, and embedded concurrency-choice cases. |
| `test_dashboard_client.py` | Browser URL, authenticated preflight header, snapshot subscription, and replay cursor tests. |
| `test_notifications.py` | Durable route-fidelity delivery, restart deduplication, and retry-state tests. |
| `test_service.py` | Later-turn evidence, authenticated dirty authorization, failed-preflight, serial control, binding, and notification tests. |
| `test_store.py` | SQLite restart continuity, owner-only permissions, and replay activity deduplication tests. |
