# DOX — packages/orchestrator-plugin/src

| File | Purpose |
| ------ | --------- |
| `bridge/` | Pi-side integration-history bridge; see `bridge/AGENTS.md`. |
| `client/` | Project orchestrator board and controls; see `client/AGENTS.md`. |
| `configSchema.json` | Dashboard plugin configuration schema for bounded polling interval. |
| `server/` | Server C lifecycle and worktree controls; see `server/AGENTS.md`. |
| `shared/` | Client/server UI data contracts; see `shared/AGENTS.md`. |
