{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "allowedRoots": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Canonical Server C workspace roots Hermes may register or mutate."
    },
    "worktreeRoot": {
      "type": "string",
      "description": "Optional worktree parent; defaults under the first allowed root."
    },
    "initCommand": {
      "type": "string",
      "description": "Optional operator-configured shell command run before Pi starts."
    }
  },
  "additionalProperties": false
}
